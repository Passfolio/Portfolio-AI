services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: passfolio
      POSTGRES_PASSWORD: passfolio
      POSTGRES_DB: passfolio
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
